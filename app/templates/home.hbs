{{view "navBar"}}
<div class="container">
    <div class="row">
        <div class="col-md-9 main bootcards-cards">
            <p class="lead">Make Crisp Notes. Not War.</p>
            {{note-card}}
        </div>
        {{view "sideBar"}}
    </div>
</div>
